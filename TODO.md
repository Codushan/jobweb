# TODO - Fix errors

- [x] Identify runtime/build/lint errors from logs.
- [ ] Fix missing `eslint` dev dependency so `npm run lint` works.
- [ ] Address dev-server conflict (PID/port 3000 in use) by stopping old process when needed.
- [ ] Re-run `npm run lint` to confirm.
- [ ] Re-run `npm run build` to confirm build success remains.

